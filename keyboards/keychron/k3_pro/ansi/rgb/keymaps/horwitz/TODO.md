* run clang-format across all horwitz keymap source files \(create a `.clang-format` config first; large diff, so do as a standalone commit\)
* investigate QMK test framework for horwitz keymap; target: process_record_user_ccp (stateful key→RGB logic) and process_record_user_gcp
